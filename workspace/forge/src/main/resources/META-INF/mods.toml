modLoader="javafml"
loaderVersion="[${FORGE_LOADER_VERSION},)"
license="${MOD_LICENSE}"
issueTrackerURL="https://discord.apexstudios.dev/"

[[mods]]
	modId="${MOD_ID}_complete"
	version="${MOD_VERSION}"
	displayName="${MOD_NAME}"
	updateJSONURL="https://api.modrinth.com/updates/fantasy-furniture/forge_updates.json"
	displayURL="https://apexstudios.dev/fantasys-furniture"
	logoFile="banner.png"
	credits="Apex - Programming"
	authors="ApexStudios"
	description="${MOD_DESCRIPTION}"
	displayTest="MATCH_VERSION"

[[dependencies."${MOD_ID}_complete"]]
	modId="forge"
	mandatory=true
	versionRange="${FORGE_VERSION_RANGE}"
	ordering="AFTER"
	side="BOTH"
[[dependencies."${MOD_ID}_complete"]]
	modId="minecraft"
	mandatory=true
	versionRange="${MINECRAFT_VERSION_RANGE}"
	ordering="AFTER"
	side="BOTH"
[[dependencies."${MOD_ID}_complete"]]
	modId="${APEXCORE_ID}"
	mandatory=true
	versionRange="${APEXCORE_VERSION_RANGE}"
	ordering="AFTER"
	side="BOTH"
[[dependencies."${MOD_ID}_complete"]]
	modId="${MOD_ID}"
	mandatory=true
	versionRange="[${MOD_VERSION},)"
	ordering="AFTER"
	side="BOTH"
